{
  "name": "angular-schedule",
  "private": true,
  "version": "0.0.1",
  "description": "Schedule on AngularJS",
  "main": "index.html",
  "devDependencies": {
    "bower": "^1.3.1",
    "browser-sync": "^2.13.0",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-concat-css": "^2.3.0",
    "gulp-sass": "^2.3.2",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
